# 安全边界详解

## 必须确认（不得假设）

以下场景必须获得用户明确确认后才能推进，无论表面信息多么充分：

| 场景 | 为什么不能假设 | 确认要点 |
|------|--------------|---------|
| **生产环境数据** | 误操作可能造成数据丢失或泄露 | 操作范围、备份状态、回滚方案 |
| **用户隐私/敏感信息** | 涉及法律合规（GDPR/PIPL 等） | 数据分类、存储位置、访问控制 |
| **付费 API/云服务** | 可能产生不可预期的费用 | 调用频率上限、费用预算、熔断策略 |
| **不可逆操作** | 删除、覆盖、发布后无法回退 | 是否可以预览/演习、回退窗口 |
| **权限/认证/授权** | 安全漏洞的最高发区 | 现有认证体系、权限模型、审计要求 |
| **安全合规要求** | 行业监管可能强制特定实现 | 适用法规、审计日志、加密标准 |

## 建议确认（有安全默认值）

以下场景有常规默认值可安全推进，但确认后可避免返工：

| 场景 | 默认假设 | 影响 |
|------|---------|------|
| 性能目标 | 满足基本可用即可，不做过早优化 | 若实际有高并发要求，架构可能需要调整 |
| 错误处理粒度 | 关键路径完整处理，非关键路径记录日志 | 生产环境可能需要更细粒度的告警 |
| 测试覆盖 | 核心逻辑单测，边角情况手动验证 | CI/CD 流程可能需要更高覆盖率 |
| 日志/监控 | 使用项目现有方案，不加额外埋点 | 排查问题时可能需要更详细的上下文 |
| 国际化 | 默认中文，不预留 i18n | 若要出海，需要改造字符串管理 |

## 可安全假设

以下场景符合业界惯例，可直接假设并推进：

- 优先使用项目已有的技术栈、目录结构、测试框架
- 优先选择可逆、低成本、可测试的实现路径
- 错误处理优先使用项目现有模式
- 代码风格遵循项目现有约定（ESLint/Prettier 配置）

## 安全假设模板

输出安全假设时采用以下格式：

> **假设：** 采用 [具体做法]
> **理由：** [为什么合理——引用项目现有模式或行业惯例]
> **影响：** [如果假设不对会导致什么]
> **验证方式：** [如何快速验证或调整]
