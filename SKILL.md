---
name: clarify-hidden-context
description: "在设计方案或编写代码前，运用乔哈里视窗“隐藏区”视角识别会改变实现路径的未说明上下文，按影响排序问题与默认假设，并在用户补充后形成可执行方案。适用于功能开发、工具构建、AI 应用、系统设计、代码优化和 vibe coding；当用户提到乔哈里视窗、隐藏区、隐藏信息、隐含需求、需求澄清、补齐上下文、先别写代码或先问关键问题时使用。"
---

# Clarify Hidden Context

## 目标

运行“先澄清、后实施”的两阶段工作流。在输出架构、实现步骤或代码前，先找出用户可能已经掌握但尚未说明、且会显著改变实现结果的信息。

把“隐藏区”作为需求澄清的类比，不要进行心理分析。始终使用“可能”“尚未明确”等措辞，不要断言用户一定知道某项信息。

## 第一阶段：识别隐藏上下文

### 1. 盘点已知事实

- 提取用户明确给出的任务、目标、背景、约束、技术栈和验收要求。
- 区分已知事实、合理推断和未知信息；不要把推断写成事实。
- 检查用户提供的仓库、代码、配置、文档或附件，先自行获取可直接发现的信息。不要询问能够从现有材料可靠得出的事实。

### 2. 筛选决策性未知信息

从以下维度中只选择与当前任务相关、且不同答案会改变决策的项目：

- 功能目标、范围、MVP 边界和完成标准
- 目标用户、使用场景、典型路径和交互形态
- 语言、框架、外部 API、部署、性能、延迟和成本约束
- 现有代码、模块、接口、数据结构、历史尝试和已知问题
- 原型速度、工程化程度、可读性、性能和自动化偏好
- 单人或多人协作、Git、测试、文档、CI/CD 和发布流程
- 稳定性、限流、费用、隐私、安全、合规和重构风险

不要机械覆盖全部维度。为每个候选项说明：

1. 尚未明确的内容是什么。
2. 为什么它会影响结果。
3. 不同答案会改变哪项范围、架构、实现或验收决策。

### 3. 划分优先级

使用以下标准分级：

- **必须确认**：不同答案会导致互不兼容的范围、验收标准或架构；涉及生产环境、敏感数据、权限、安全、合规、付费服务或不可逆操作；或者缺少它就无法可靠验证结果。
- **建议确认**：会明显影响质量、体验、可维护性、性能或交付效率，但采用常规默认值仍可安全推进。
- **可以假设**：影响较小、容易验证和回退，且存在符合现有上下文的常规默认值。

不要把所有未知信息都标成“必须确认”。

### 4. 收敛问题

- 优先提出高影响、低回答成本的问题。
- 首轮最多提出 5 个问题，通常控制在 3–4 个；合并高度相关的问题。
- 让问题具体、可回答。适合时提供 2–3 个选项，并标出建议默认值及其取舍。
- 不要重复询问用户已说明或可从现有材料中查明的信息。
- 如果没有必须确认的问题，不要为了完成格式而制造问题；明确说明可以按默认假设继续。

### 5. 提出默认假设

- 优先选择安全、可逆、低成本、可测试且符合现有技术栈的默认值。
- 为每项假设说明它会带来的范围或实现影响，以及后续如何验证或替换。
- 不要用默认假设绕过涉及安全、隐私、合规、真实费用、破坏性操作或外部发布的必要确认。

## 第一阶段输出格式

严格使用以下五部分，并使用用户当前的语言：

### 一、目前已经明确的信息

简要总结任务、背景、约束和目标。只陈述事实，并单独标出有证据支持的推断。

### 二、可能存在的“隐藏区”信息

仅列出高影响的未明确信息。为每项说明“为什么重要”和“会改变什么决策”。

### 三、信息优先级

分别列出“必须确认”“建议确认”“可以假设”。允许某一组为空，但要明确写出。

### 四、需要我补充的问题

给出精简、按影响排序的问题清单。对适合采用默认值的问题标出建议选项。

### 五、默认假设

列出可安全采用的临时假设、影响和验证方式。若某项不能安全假设，明确说明原因。

不要在第一阶段夹带最终架构、完整实现步骤、代码、文件修改或看似默认假设的变相方案。

- 用户未授权采用假设时：输出第五部分后停止并等待回答。
- 用户已在同一请求中明确说“按最佳判断继续”“按默认假设继续”“不用等我确认”或同等含义时：把第五部分中的安全默认值视为已接受，并在同一轮立即进入第二阶段。不要再次请求许可，也不要停在澄清报告末尾。

## 第二阶段：形成方案或实施

在用户补充信息或明确接受默认假设后继续。接受可以发生在上一轮，也可以由用户在最初请求中预先授权：

1. 合并原始事实、用户的新答案和已接受的假设；保持三者边界清晰。
2. 仅在仍存在会使方案失效的缺口时，再提出一小轮阻塞性问题。
3. 信息充分时，直接按用户要求输出或实施具体方案。
4. 根据任务需要包含架构决策、文件级改动、关键代码、测试、验收和发布步骤；不要堆砌无关章节。
5. 说明仍未解决的风险和假设，但不要重复整份第一阶段报告。
6. 如果用户只要求方案、评审或诊断，不要擅自修改代码；如果用户要求构建或修改，则实施并验证。

## 防偏规则

- 不要把“隐藏区”误作“盲区”，也不要评价用户的认知能力。
- 不要把七类维度变成通用问卷；保留真正会改变决策的项目。
- 不要以澄清为由无限阻塞。用户要求“按最佳判断继续”时，采用已声明的安全默认值并推进。
- 不要忽略现有项目约定。优先沿用仓库已经采用的技术栈、结构和测试方式，除非用户明确要求改变。
- 不要承诺无法验证的完成标准。把验收条件写成可观察、可测试的结果。
