---
name: clarify-hidden-context
description: 在方案设计或编码前，逐一追问用户未明说但会改变实现路径的关键上下文，将结论写回项目文件。适用于功能开发、系统设计、代码优化；当用户提到乔哈里视窗、隐藏区、隐含需求、需求澄清、先别写代码时使用。
disable-model-invocation: true
---

# Clarify Hidden Context

## 核心原则

1. **只追问会改变决策的问题** — 不问能从代码/文档中查到的，不问答案不影响实现路径的
2. **一次只问一个** — 等待回答后再继续，不堆砌问题清单
3. **按优先级区分** — [必须确认] 涉及安全/付费/不可逆/权限；[建议确认] 有安全默认值；其余列假设即可
4. **写回项目文件** — 结论实时写入 `CONTEXT.md` 或 `docs/decisions/`，产出可跨会话复用
5. **与代码交叉验证** — 用户声称的规则与代码矛盾时立即指出
6. **不制造问题** — 无阻塞项时列出安全假设，引导用户直接确认推进

## 安全边界

以下情况**必须确认，不得假设**：生产数据/用户隐私 · 付费 API/云服务 · 不可逆操作（删除/发布）· 权限/认证/授权 · 安全合规要求

## 两阶段工作流

### 第一阶段：逐一澄清

1. 从代码和现有文件中自行获取可查信息（技术栈、项目结构、已有模块）
2. 筛出真正会改变决策的未知项，按优先级排序（首轮 ≤5 个）
3. **逐个提问**，每个问题说清"不同答案会导致什么不同决策"，适合时给 2-3 个选项并标出推荐
4. 合并高度相关问题，让每个问题可具体回答
5. 无阻塞性问题时，列出安全假设，用户回复"继续"即按假设推进

⚠️ **第一阶段必须停止等待用户回复，不得在同一轮进入第二阶段。**

### 第二阶段：形成方案或实施

用户补充信息或确认默认假设后：
- 合并原始事实、新答案与已接受的假设，保持三者边界清晰
- 若要求方案/诊断：输出分析结论，不擅自改代码
- 若要求构建/修改：实施、验证并报告结果
- 标注仍未解决的风险，不重复整份澄清报告

## 写回规则

- 术语定义 → 项目根 `CONTEXT.md`（仅写入已确认结论，不写假设）
- 架构决策 → `docs/decisions/YYYY-MM-DD-<slug>.md`
- 不写回：假设、待定项、技术实现细节

## 参考文件

需要时加载：
- 7 个决策维度及筛选标准 → [dimensions.md](dimensions.md)
- 输出格式示例与反模式 → [format-guide.md](format-guide.md)
- 安全边界详解与默认假设指南 → [safety-rules.md](safety-rules.md)
