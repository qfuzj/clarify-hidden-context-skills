---
name: clarify-hidden-context
description: "在方案设计或编码前，识别用户未明说但会改变实现路径的关键上下文，按影响排序问题，补齐信息后形成可执行方案。适用于功能开发、工具构建、AI 应用、系统设计、代码优化和 vibe coding；当用户提到乔哈里视窗、隐藏区、隐含需求、需求澄清、补齐上下文、先别写代码或需要先问关键问题时使用。"
---

# Clarify Hidden Context

## 目标

运行"先澄清、后实施"的两阶段工作流。在输出具体方案或代码前，先识别用户可能掌握但尚未说明、且会显著改变实现结果的关键信息。

**核心原则**：
- 只关注会改变决策的信息，不做信息收集练习
- 优先从现有材料中自行获取信息，不要询问能直接查到的内容
- **强制执行澄清阶段**：无论任何情况，必须先完成第一阶段的澄清输出，等待用户确认后才能进入第二阶段
- 把"隐藏区"仅作为需求澄清的类比，不要进行心理分析
- 始终使用"可能""尚未明确"等措辞，不要评价用户的认知能力或断言用户一定知道某信息

---

## 第一阶段：识别关键未知信息

### 1. 盘点已知与待查

**已知事实**：提取用户明确给出的任务、目标、约束、技术栈和验收标准。

**可自查信息**：检查仓库代码、配置文件、package.json、README、已有实现，先自行获取可直接发现的技术栈、项目结构、现有模块等信息。

**真正未知**：仅保留无法从现有材料推断、且不同答案会导致不同实现路径的信息。

### 2. 筛选决策性问题

从以下维度中**仅选择与当前任务相关且会改变决策的项目**：

| 维度 | 典型决策性问题示例 | 决策影响 |
|------|-------------------|----------|
| **范围与边界** | MVP 包含哪些场景？是原型还是生产就绪？完成标准是什么？ | 决定实现深度、测试覆盖、错误处理粒度 |
| **使用场景** | 用户如何触发？典型输入是什么？边界情况如何处理？ | 影响 API 设计、参数验证、边界处理 |
| **技术约束** | 必须使用特定框架/库？性能/延迟要求？部署环境限制？ | 决定技术选型、架构模式、优化方向 |
| **现有系统** | 有可复用的模块/接口？历史尝试为何失败？已知技术债？ | 影响是否重用、改造还是新建 |
| **质量权衡** | 优先速度还是工程化？可读性还是性能？重构风险容忍度？ | 决定实现风格、抽象层次、交付节奏 |
| **协作流程** | 单人还是多人？需要测试/文档？有 CI/CD 流程？ | 影响代码组织、提交粒度、验收方式 |
| **风险控制** | 涉及生产数据？外部 API 费用？安全/合规要求？不可逆操作？ | 决定是否需要审批、回滚方案、监控埋点 |

**筛选标准**：
- ✅ 不同答案会导致互不兼容的架构或范围
- ✅ 涉及安全、合规、付费、破坏性操作或外部发布
- ✅ 缺少它就无法验证结果是否符合预期
- ❌ 影响很小且有安全默认值可用
- ❌ 能从现有代码或文档中直接查明

### 3. 划分优先级并收敛

**必须确认**：不同答案导致互不兼容的方案；或涉及安全、合规、付费、生产、不可逆操作或外部发布。

**建议确认**：明显影响质量、性能、可维护性，但有常规默认值可安全推进。

**可以假设**：影响较小、易验证回退，有符合上下文的惯例默认值。

**收敛规则**：
- 首轮最多 3–5 个问题，优先高影响、低回答成本的问题
- 合并高度相关的问题，让每个问题具体可回答
- 适合时提供 2–3 个选项，标出建议默认值及权衡
- 如果没有必须确认的问题，说明可按默认假设继续，不要制造问题

### 4. 提出安全默认假设

为"建议确认"和"可以假设"的项目给出默认值：
- 优先选择：安全、可逆、低成本、可测试、符合现有技术栈
- 说明每项假设的影响和后续验证方式
- 每个假设都要能说清楚：采用什么、为什么合理、如何验证或调整

**安全边界**：以下情况不得使用默认假设，必须明确确认：
- 涉及生产环境数据、用户隐私、敏感信息
- 调用付费外部 API、云服务、第三方接口
- 执行不可逆操作（删除、覆盖、发布）
- 修改权限、认证、授权逻辑
- 影响安全、合规、审计要求的决策

---

## 第一阶段输出格式

**简化为三部分**（使用用户当前语言）：

### 一、当前已明确的信息

简要总结任务、目标、约束。只陈述事实；推断须单独标出并说明证据，不要把推断写成事实。

### 二、关键未明确信息与问题

按优先级分组列出：

**[必须确认]**（如有）
- 问题 1：xxx？（为什么重要：会改变 yyy 决策）
- 问题 2：xxx？（选项 A/B/C，建议 A 因为 zzz）

**[建议确认]**（如有）
- 问题 3：xxx？（默认假设：采用 www，影响：xxx，可后续调整）

**[可安全假设]**（如有）
- 假设 1：采用 xxx（理由：yyy，验证方式：zzz）
- 假设 2：采用 xxx（理由：yyy，验证方式：zzz）

### 三、下一步

**情况 A：存在必须确认的问题**
→ "请补充以上 [必须确认] 部分的信息，我将基于你的回答和已列出的安全假设形成具体方案。"

**情况 B：无必须确认，仅有建议确认/可安全假设**
→ "以上假设均可安全采用。你可以直接回复'继续'，我将按这些假设推进；或补充任何你认为重要的信息。"

**情况 C：信息看似充分**
→ "虽然信息看似充分，但为确保理解一致，请确认以上假设或直接回复'继续'。"

**⚠️ 关键规则：无论哪种情况，第一阶段都必须在输出"下一步"后立即停止，等待用户回复。绝不允许在同一轮直接进入第二阶段。**

---

## 第二阶段：形成方案或实施

**触发条件**（满足其一）：
1. 用户补充了必须确认的信息
2. 用户明确接受默认假设：
   - 关键词："继续""按你的判断""按默认假设""不用等我""直接做吧""你看着办""用最佳实践"
   - 上下文：用户回复简短且无新增约束

**⚠️ 第二阶段只能在用户明确回复后触发，不允许在第一次调用 skill 时直接跳过澄清阶段。**

**执行步骤**：
1. 合并原始事实、用户新答案和已接受的假设，保持三者边界清晰
2. 仅在仍存在阻塞性缺口时再提一小轮问题
3. 信息充分时，直接按用户要求输出或实施：
   - 如用户要求方案/评审/诊断：输出分析结论，不擅自改代码
   - 如用户要求构建/修改：实施、验证并报告结果
4. 根据任务需要包含架构决策、改动范围、关键代码、测试、验收步骤；不堆砌无关章节
5. 说明仍未解决的风险和假设，但不重复整份澄清报告

---

## 执行要点

### 强制澄清规则

**⚠️ 核心要求：无论任何情况，都必须先执行第一阶段的澄清，输出三部分内容（已明确信息、未明确信息与问题、下一步），然后停止等待用户回复。**

即使遇到以下情况也不能跳过澄清：
- 用户给出看似完整的需求、验收标准、技术约束
- 任务看起来是修复明确的 bug、实现标准功能
- 用户要求"按你的判断直接做""不用问我""直接实现"

这些情况下，仍需输出澄清报告，但可以在 [可安全假设] 中列出更多默认假设，并引导用户快速确认（"请回复'继续'即可按默认假设推进"）。

### 防偏规则

- ❌ 不要把"隐藏区"误作"盲区"，不要进行心理分析或评价用户的认知能力；把"隐藏区"仅作为需求澄清的类比
- ❌ 不要把七类维度变成通用问卷，保留真正会改变决策的项目
- ❌ 不要以澄清为由无限阻塞，用户要求继续时采用已声明的安全默认值
- ❌ 不要忽略现有项目约定，优先沿用已有技术栈、结构、测试方式
- ❌ 不要承诺无法验证的完成标准，把验收条件写成可观察、可测试的结果
- ❌ 不要在第一阶段夹带最终架构、完整步骤、代码或变相方案

### 质量检查

输出前自检：
- 每个问题都能说清"为什么会改变决策"
- 每个假设都有明确的影响范围和验证方式
- 输出结构清晰，不重复不遗漏
- 优先级划分合理，不把所有未知都标为"必须确认"
- 已从现有材料中获取的信息不再询问
