# 输出格式参考

## 原则

- 使用用户当前语言（中文/英文）
- 格式服务于决策，不是填写表格
- 每个问题都说明"不同答案会改变什么"

## 对话式提问（推荐）

一次一个问题，逐个追问：

```
你提到了"用户系统"，现有代码中已有 Customer 和 Account 两个概念——
你指的是哪一个？这会影响是复用现有模块还是新建。
```

```
这个功能需要调用外部支付 API 吗？如果是，我需要先确认
API Key 管理方式和费用上限。这决定了是否需要加审批流程。
```

## 汇总式输出（当问题较多时）

```
### 当前已明确
- 目标：构建内部工具的价格计算器
- 技术栈：React + TypeScript（从 package.json 确认）
- 用户：仅内部团队使用

### 待确认（按优先级）

[必须确认]
1. 计算结果会触发实际扣款吗？
   → 如果是，需要审批流程和回滚方案；如果仅展示，实现简单很多

[建议确认]
2. 价格规则由谁维护？默认假设：硬编码在前端，后续可抽配置

[可安全假设]
- 不需要用户登录（内部工具，IP 白名单即可）
- 单页应用，不需要路由

### 下一步
请补充 [必须确认] 的信息，其余可按默认假设推进。
```

## 反模式

❌ 把七个维度变成问卷逐一询问
❌ 问能从代码中查到的信息（如"项目用的是 React 还是 Vue？"）
❌ 一次抛出 5+ 个问题让用户回答
❌ 用"你看这里有个隐藏区"做心理分析
❌ 在第一阶段直接给出完整实现方案或代码
